
// 카멜 케이스 (camelCase) : 주로 변수, 함수명에 사용
var firstName;

// 스네이크 케이스 (snakeCase)
var first_name;

// 파스칼 케이스 (PascalCase) : 주로 생성자 함수, 클래스 이름에 사용
var FirstName;

// 헝가리언 케이스(typeHungarianCase)
var strFirstName;
var $elem = document.getElementById('myId');
var observable$ = fromEvent(document, 'click'); // RxJs 옵저버블
